# Replay Manifests

Place deterministic replay definitions here.

Naming: `replay_manifest_<artifact_id>_<version>.json`

Required fields:
- artifact_id / property_id
- input_hash / expected_hash
- commit_sha
- environment_hash
- command
- result
- status
